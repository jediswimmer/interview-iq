# Required — Claude API for coaching analysis
ANTHROPIC_API_KEY=sk-ant-...

# Optional — Deepgram for lowest-latency STT (streaming WebSocket)
# If not set, falls back to faster-whisper (local, runs on Apple Silicon)
DEEPGRAM_API_KEY=
